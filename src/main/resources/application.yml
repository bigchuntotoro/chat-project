server:
  port: 8086

spring:
  datasource:
    url: jdbc:mariadb://100.88.187.37:3306/reactdb?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: totoro
    password: totoro
    driver-class-name: org.mariadb.jdbc.Driver
  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: I1su4VJyVqMgmLJ_Uha8
            client-secret: QgSeqSBb0I
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            scope: name,email,profile_image
            client-name: Naver
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: your_jwt_super_secret_key_which_should_be_long_enough_for_hs512_algorithm
  expiration: 86400000

logging:
  level:
    org.springframework.security: DEBUG