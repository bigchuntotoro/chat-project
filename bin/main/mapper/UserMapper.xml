<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chat.mapper.UserMapper">

    <select id="findByEmail" resultType="com.example.chat.domain.User">
        SELECT id, email, name, profile_image AS profileImage, status, created_at AS createdAt
        FROM users
        WHERE email = #{email}
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, name, profile_image, status, created_at)
        VALUES (#{email}, #{name}, #{profileImage}, 'ONLINE', NOW())
    </insert>

    <update id="updateUserStatus">
        UPDATE users SET status = #{status} WHERE email = #{email}
    </update>

</mapper>